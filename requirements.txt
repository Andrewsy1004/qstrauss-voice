faster-whisper>=1.0.0
# onnxruntime es transitivo de faster-whisper, pero vad.py depende de él
# directamente (Silero VAD). Se declara explícito para no romper si
# faster-whisper cambia sus dependencias.
onnxruntime>=1.16.0

# Lectura del diccionario hunspell (resources/dic/) para el guard lexico de
# terms.py. Es puro Python, asi que empaqueta sin problemas y da el mismo
# comportamiento en macOS y en Windows.
spylls>=0.1.7
sounddevice>=0.4.6
numpy>=1.24.0
pyperclip>=1.8.2
pynput>=1.7.6

# Mac only — mlx-whisper uses Metal GPU, 10-20x faster than CPU on Apple Silicon
mlx-whisper>=0.4.0; sys_platform == "darwin"
rumps>=0.4.0; sys_platform == "darwin"

# Windows only
pystray>=0.19.4; sys_platform == "win32"
Pillow>=10.0.0; sys_platform == "win32"
pywebview>=4.0; sys_platform == "win32"
